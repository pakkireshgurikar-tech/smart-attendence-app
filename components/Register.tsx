import React from 'react';

// This component is legacy and has been replaced by AdminRegister.tsx and inline registration forms.
// The content has been removed to prevent component name collisions and React errors.
const LegacyRegister: React.FC = () => {
  return null;
};

export default LegacyRegister;
