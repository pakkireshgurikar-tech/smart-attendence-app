import React from 'react';

// This component is legacy and its functionality is now part of other dashboard components.
// The content has been removed to prevent component name collisions and React errors.
const LegacySchedule: React.FC = () => {
  return null;
};

export default LegacySchedule;
